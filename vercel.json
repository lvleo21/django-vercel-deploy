{
    "builds": [{
        "src": "config/wsgi.py",
        "use": "@natanffs/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "(.*)",
            "dest": "config/wsgi.py"
        }
    ]
}